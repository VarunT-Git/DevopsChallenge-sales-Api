steps:
- task: Docker@2
  displayName: Build
  inputs:
    dockerfile: src/DevOpsChallenge.SalesApi/Dockerfile
    command: build
    tags: $(Build.BuildId)
    buildContext: $(Build.Repository.LocalPath)

- publish: manifests
  artifact: manifests